import './css/Navbar.css';

/**
 * Returns the navbar shown at the top of the web app.
 */
function Navbar() {
  return (
    <nav className='navbar'>
      <span className='nav-text'>3D Object Viewer</span>
    </nav>
  );
}

export default Navbar;
